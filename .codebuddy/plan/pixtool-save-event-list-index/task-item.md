# 实施计划

- [ ] 1. 梳理并固化现有 `build-index` 技术路线文档
   - 补充当前 `build-index` 的 CLI 入口、MCP 入口、参数流转、旧索引构建数据来源和数据库输出路径说明
   - 明确旧流程中需要被 `save-event-list` CSV 导出与导入替换的具体步骤
   - _需求：1.4、3.1、3.2、8.1_

- [ ] 2. 抽象 `build-index` 的事件列表导出接口
   - 在现有 `build-index` 流程中增加可测试的事件列表导出函数或服务边界
   - 实现默认 `pixtool.exe` 路径 `C:\Program Files\Microsoft PIX\2603.25\pixtool.exe` 的解析与可覆盖逻辑
   - 实现 `capture_path`、`export_dir`、`refresh` 与事件列表 CSV 输出路径的推导
   - _需求：1.1、4.2、4.3、4.5、6.4_

- [ ] 3. 实现 `pixtool.exe save-event-list` 调用逻辑
   - 生成 `save-event-list <filename>` 命令并在提供 counters 模式时追加 `--counters=<pattern>`
   - 校验 `pixtool.exe` 是否存在且可执行
   - 捕获命令退出码、标准输出和错误输出，并返回可诊断的阶段化错误
   - _需求：1.1、1.2、1.3、1.5、1.6、7.1_

- [ ] 4. 实现 CSV 事件列表解析与字段映射
   - 编写 CSV 读取逻辑，支持事件 ID、事件名称、层级、时间、持续时间和 counters 字段识别
   - 对缺失可选字段提供空值或默认值处理
   - 对缺失关键字段返回包含字段名和 CSV 路径的明确错误
   - _需求：2.1、2.2、2.3、2.4、7.2_

- [ ] 5. 实现基于 CSV 的数据库构建流程
   - 修改或新增数据库 schema 初始化和导入逻辑，使 `build-index` 能将 CSV 事件列表写入约定数据库
   - 保证导入后的关键字段可被现有 SQL 查询使用
   - 在成功结果中输出或返回 CSV 路径、数据库路径和刷新状态
   - _需求：2.1、2.5、3.5、5.1、5.2_

- [ ] 6. 替换 `build-index` 旧索引构建步骤
   - 将现有事件索引导出或扫描步骤替换为 `save-event-list` CSV 导出与数据库导入流程
   - 避免保留旧事件来源与新 CSV 来源并行写入造成数据不一致
   - 对仍需非 CSV 数据来源的后续工具保留清晰边界和错误提示
   - _需求：1.4、5.3、5.4、5.5_

- [ ] 7. 统一 CLI 与 MCP 的 `build-index` 参数和返回结果
   - 调整 CLI 参数、帮助信息与 MCP schema，使二者参数名称、默认值和用途一致
   - 移除或避免引入无法保持 CLI/MCP 行为一致的参数
   - 统一成功输出与失败返回中的核心字段和错误分类
   - _需求：3.1、3.2、3.3、3.4、3.5、4.1、4.2、4.4_

- [ ] 8. 实现缓存、刷新和输出目录处理
   - 判断事件列表 CSV 与数据库是否可复用
   - 在显式刷新或捕获文件更新时重新执行 `save-event-list` 并重建数据库
   - 自动创建输出目录，并在目录创建、CSV 解析或数据库写入失败时返回明确错误
   - _需求：6.1、6.2、6.3、6.5、7.3、7.4_

- [ ] 9. 补充自动化测试与模拟执行能力
   - 添加 CSV 解析测试，覆盖正常 CSV、缺失可选字段、缺失关键字段和 counters 字段
   - 添加 `save-event-list` 命令构造与失败处理测试，避免测试环境依赖真实 `pixtool.exe`
   - 添加 CLI 与 MCP `build-index` 参数语义和关键返回字段一致性验证
   - 添加数据库导入后关键 SQL 查询可运行的回归测试
   - _需求：7.5、9.1、9.2、9.3、9.4、9.5_

- [ ] 10. 更新新方案技术文档和实现后说明
   - 记录 `build-index` 使用 `save-event-list` 导出 CSV、解析 CSV、写入数据库的技术路线
   - 记录 schema 映射、已知限制、降级行为和后续工具的数据来源边界
   - 实现完成后同步当前代码实际逻辑、关键入口、数据流和测试方式
   - _需求：5.3、8.2、8.3、8.4、8.5_
